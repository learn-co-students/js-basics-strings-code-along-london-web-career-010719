// Write your code in this file!
let currentUser = 'Chris Camacho';

let welcomeSuffix = currentUser + '!';

let welcomeMessage = 'Welcome to Flatbook, ' + welcomeSuffix

let excitedWelcomeMessage = welcomeMessage.toUpperCase();

let shortGreeting = 'Welcome, ' + currentUser.substr(0,1) + '!'
